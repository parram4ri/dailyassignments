let fs = require('fs');
let path = require('path');

//Student Code

describe('Requirements', function() {
  it("i should only increment once in the do while loop", function() {
    // Failure message: 
    // Make sure you increment i and that the loop only runs once.
    expect(i).toBe(6);
  });
});
